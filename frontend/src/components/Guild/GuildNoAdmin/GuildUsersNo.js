// import React from 'react';
// import GuildUserNo from './GuildUserNo';

// const GuildUsersNo = ({
//     admin, // 길드 관리자
//   users, // 일반 길드원
  
// }) => {
//     return (
//         <div className='GuildUsersNo'>
//             {users.map((user) => <GuildUserNo key={user.id} user={user} length={users.length} admin={admin}  />)}
//         </div>
//     );
// };

// export default GuildUsersNo;