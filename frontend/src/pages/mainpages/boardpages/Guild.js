import React from 'react';
import Header from './../../../components/Header';

const Guild = () => {
    return (
        <div>

            <Header data={'Guild'} />
        </div>
    );
};

export default Guild;
